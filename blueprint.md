# 🎞️ 7080 추억의 사진관 - 청사진

## 🌟 개요

"7080 추억의 사진관"은 사용자가 업로드한 사진을 1970년대와 1980년대 레트로 스타일(옛날 교복, 교련복 등)로 변환해주는 웹 애플리케이션입니다. React와 Vite를 기반으로 구축되며, Replicate API를 활용하여 이미지 변환 기능을 제공합니다.

## ✨ 주요 기능 및 디자인

- **메인 페이지:**
    - "7080 추억의 사진관"이라는 헤더와 "그 시절, 우리가 입었던 그 교복 그대로."라는 부제를 표시합니다.
    - 레트로 감성의 디자인을 적용하여 사용자 경험을 향상시킵니다.
    - 저작권 및 기술 문의 정보를 포함하는 푸터를 표시합니다.

- **이미지 업로드 및 미리보기:**
    - 사용자는 자신의 사진 파일을 업로드할 수 있습니다.
    - 업로드된 사진은 화면에 미리보기로 표시됩니다.

- **스타일 선택:**
    - 사용자는 변환할 스타일을 선택할 수 있습니다. (예: 검정 옛날 교복, 얼룩무늬 교련복)
    - 드롭다운 메뉴를 통해 간편하게 스타일을 선택합니다.

- **이미지 변환:**
    - "과거로 돌아가기" 버튼을 클릭하면 이미지 변환 프로세스가 시작됩니다.
    - 변환 중에는 "타임머신 가동 중..."이라는 메시지와 함께 로딩 상태를 표시합니다.
    - Replicate API를 호출하여 선택된 스타일로 이미지를 변환합니다.
        - **주의:** API 키는 실제 서비스 시 백엔드에서 안전하게 관리해야 합니다.

- **결과 표시 및 다운로드:**
    - 이미지 변환이 완료되면 결과 이미지를 화면에 표시합니다.
    - 사용자는 변환된 이미지를 다운로드할 수 있습니다.

## 📝 개발 계획

1.  **초기 설정:**
    - **(완료)** 기존 Vite + React + TypeScript 프로젝트 환경을 활용합니다.

2.  **UI/UX 구현:**
    - `src/App.tsx` 파일을 수정하여 전체적인 UI 구조(헤더, 입력 섹션, 결과 섹션, 푸터)를 잡습니다.
    - `src/App.css` 파일에 레트로 감성의 스타일(폰트, 색상, 레이아웃)을 추가하여 디자인을 완성합니다.

3.  **핵심 기능 구현:**
    - 사진 업로드 시 미리보기를 생성하는 로직을 구현합니다.
    - 스타일 선택 드롭다운의 상태를 관리합니다.
    - '변환하기' 버튼 클릭 시 Replicate API를 호출하는 비동기 로직을 구현합니다. (폴링 로직은 추후 구현 필요)
    - 로딩 상태를 관리하여 사용자에게 피드백을 제공합니다.
    - (임시) 변환 결과를 표시하는 UI를 구현합니다.

4.  **코드 리팩토링 및 타입스크립트 적용:**
    - 제공된 JSX 코드를 TypeScript(TSX)에 맞게 변환합니다. (state, event handler 등에 타입 추가)
    - 코드의 가독성과 유지보수성을 높입니다.
